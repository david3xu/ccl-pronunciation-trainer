import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ptetrainer.app',
  appName: 'PTE Pronunciation Trainer',
  webDir: 'dist',
};

export default config;
