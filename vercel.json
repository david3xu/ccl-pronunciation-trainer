{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "npm run extract-vocab && npm run build",
        "outputDirectory": "dist"
      }
    }
  ]
}